{
  "name": "node-social-stats",
  "version": "0.1.0",
  "description": "Get standardized social statistics for any url",
  "main": "src/module.js",
  "scripts": {
    "start": "node src/module.js",
    "dev": "babel src --watch --out-dir lib",
    "sample": "node examples/simple.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request-promise": "^1.0.2"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.1.18"
  }
}
